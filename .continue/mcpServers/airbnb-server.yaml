name: Airbnb Search and Listing Server
version: 0.0.1
schema: v1
mcpServers:
  - name: Airbnb Search and Listing Server
    command: npx
    args:
      - -y
      - "@openbnb/mcp-server-airbnb"
    env: {}
