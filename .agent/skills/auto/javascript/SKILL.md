---
name: javascript
description: "Javascript for node. 1 conventions, 2 fixes."
domain: javascript
triggers:
  - glob: "**/*.js"
  - glob: "**/*.jsx"
autoGenerated: true
enabled: true
---

# Javascript

Auto-compiled from **8 real patterns** in **node**. This skill is auto-routed to agents when working on javascript files.

## 🔧 Problem Playbooks

### problem-fix in filter.js
- const files = new Set();
+ const printErrors = (file) => {
- lines.forEach(line => {
+   console.log(`\n--- ${file} ---`);
-   const m = line.match(/(src\/lib\/services\/.*?\.ts)/);
+   lines.forEach((line, i) => {
-   if (m) files.add(m[1]);
+     if (line.includes(file)) {
- });
+       console.log(line);
- console.log(Array.from(files));
+       for(let j=1; j<5; j++) {
- 
+          if (line

**Actionable Steps:**
1. Modified 1 files

### problem-fix in analyze-ts-errors.js
- const log = fs.readFileSync('ts_errors.log', 'utf-8');
+ const log = fs.readFileSync('ts_errors.log', 'utf16le');

📌 IDE AST Context: Modified symbols likely include [fs, log, lines, fileErrors, currentFile]

**Actionable Steps:**
1. Modified 1 files

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **what-changed in filter.js — confirmed 4x** — -   if (line.includes('src/lib/services/subscriptionService.ts')) {
+   if (line.includes('src/lib/s

---
*Auto-generated by BrainSync 🧠 | 8 patterns | 2026-04-06*
