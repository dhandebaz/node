---
name: problem-fix
description: "Problem Fix for node. 39 error→fix pairs."
domain: problem-fix
composesFrom:
  - fix
triggers:
  - glob: "**/*"
autoGenerated: true
enabled: true
---

# Problem Fix

Auto-compiled from **4 real patterns** in **node**. This skill is auto-routed to agents when working on problem-fix files.

## 🔧 Problem Playbooks

### Known Error → Fix

| Error Pattern | Fix | Seen |
|-------|-----|------|
| `- - Fixed null crash in UpsellStore — prevents nul` | problem-fix in agent-rules.md | 8x |
| `const { data: { user }, error: authError } = await` | problem-fix in route.ts | 7x |
| `- - Fixed null crash in Date — prevents null/undef` | problem-fix in agent-rules.md | 7x |
| `return NextResponse.json({ error: "Unauthorized" }` | problem-fix in admin.ts | 6x |
| `- - Fixed null crash in Context — prevents null/un` | problem-fix in agent-rules.md | 6x |
| `import { withErrorHandler } from "@/lib/api/withEr` | problem-fix in route.ts | 5x |
| `import { FailureService } from "@/lib/services/fai` | problem-fix in route.ts | 5x |
| `+     } catch (error) {` | Fixed null crash in Review — prevents null/undefined runtime | 5x |
| `- - Fixed null crash in Business — prevents null/u` | problem-fix in agent-rules.md | 5x |
| `import { AppError, ErrorCode } from "@/lib/errors"` | problem-fix in walletService.ts | 4x |
| `+ - Fixed null crash in UpsellStore — prevents nul` | problem-fix in agent-rules.md | 4x |
| `+ - Fixed null crash in Date — prevents null/undef` | problem-fix in agent-rules.md | 4x |
| `File updated (external): ts_errors.log` | problem-fix in ts_errors.log | 3x |
| `+       const { data, error } = await supabase.fro` | problem-fix in flows.ts | 3x |
| `File updated (external): ts_errors.txt` | problem-fix in ts_errors.txt | 3x |
| `error: authError,` | problem-fix in route.ts | 3x |
| `-     const { error } = await supabase.from("syste` | problem-fix in controlService.ts | 3x |
| `- - Fixed null crash in Plus — avoids unnecessary ` | problem-fix in agent-rules.md | 3x |
| `- - Fixed null crash in Building — avoids unnecess` | problem-fix in agent-rules.md | 3x |
| `- - Fixed null crash in OmniGlobalConfig — prevent` | problem-fix in agent-rules.md | 3x |

## 📐 Conventions & Best Practices

### Compiled Rules
- ✅ When encountering this, fix by: Fixed null crash in BusinessType — prevents null/undefined runtime crashes (seen 2x)
- ✅ When encountering this, fix by: problem-fix in tsc_errors.txt (seen 2x)
- ✅ When encountering this, fix by: problem-fix in agent-rules.md (seen 2x)
- ✅ When encountering this, fix by: Fixed null crash in Date — prevents null/undefined runtime crashes (seen 2x)

---
*Auto-generated by BrainSync 🧠 | 4 patterns | 2026-04-10*
